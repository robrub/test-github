/**
 * Questa è una funzione di somma semplice in JavaScript.
 * Prende due parametri, a e b, che dovrebbero essere numeri.
 * Restituisce la somma di a e b.
 *
 * @param {number} a - Il primo numero da sommare.
 * @param {number} b - Il secondo numero da sommare.
 * @return {number} La somma di a e b.
 */
function add(a, b) {
  return a + b; // commento inline
}



const result = add(1, 2);
console.log("Risultato", result);
